* Login/splash
	* Need to listen for Ajax errors on trends
* My Timeline
	* Combined timeline
		* dupes still seem to get through in some cases on combined timeline (posting new msgs)
	* Need to listen for Ajax errors
	* Timeline is sometimes out of order when loading some from cache (replies and DMs). Need to resort whole list on view side, prob
* Favorites Timeline
	* Need to create
* Message Detail
	* Need to listen for Ajax errors
	* reveal IRT instead of just linking to it
	* add star to favorite button (that reflects status)
	* DMs don't show detail properly (need to detect and grab with right method)
* User Detail
	* Need to listen for Ajax errors on user data
	* Need to listen for Ajax errors on user timeline
	* grab data from /users/username.json (done)
	* Data timeline should only load on request
	* parse links, usernames, hashtags
	* make buttons functional
		* View recent posts (DONE)
			* change template for this view and display inline (DONE)
		* Search for user (DONE)
		* send reply (DONE)
		* send dm (DONE)
		* Follow (DONE)
		* block (DONE -- but API doesn't tell us blocking state, so blocked users will show "unblocked")
* Search
	* Need to listen for Ajax errors
* Preferences
	* create scene
	* add to AppMenu
* Notifications
	* banner on new posts
	* Link banners back to scene that opened them
	* raise notifications from search
* Posting
	* Need to listen for Ajax errors in posting
	* posting a dm returns the last "regular" message and prepends it to the timeline (even if dupe)
	* can still send if over char limit

SpazTwit
	* Add generic event raised on Ajax errors
	* Make sure all methods raise specific error events
	* add event for "no new messages" so view can do any necessary updates
	* do follow/unfollow calls
	* do block calls
	* complete min_age limits on refresh, and "force refresh" functionality


## UPCOMING FEATURES
	* Photo posting
	* Photo taking and posting
	* Updating location with GPS services
	* People Picker widget to integrate with contacts for posting(?)